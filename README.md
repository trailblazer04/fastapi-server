Task: PostgreSQL with Docker + FastAPI API

- Created a local PostgreSQL database using Docker Compose
- Added a table with raw SQL
- Built FastAPI endpoints:
  - `POST /add` to insert
  - `GET /users` to fetch
